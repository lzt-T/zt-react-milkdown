## 1. 撤销错误边界实现

- [x] 1.1 移除 `inclusive: false` 行内代码 schema 覆盖及其编辑器装配，恢复 Milkdown 默认包含边界。
- [x] 1.2 移除为非包含边界补充的连续输入插件、默认插件描述及相关孤儿导入。

## 2. 行内代码右边界导航

- [x] 2.1 新增行内代码右边界导航插件，第一次按右方向键时移除活动 mark 中的 `inlineCode` 并阻止跨段移动。
- [x] 2.2 将第一次退出后的原生折叠选区放到 `<code>` 元素右侧，使标签外光标具有可见反馈。
- [x] 2.3 在重新点击行内代码尾部时恢复标签内输入状态，并让第二次右方向键遵循普通导航。
- [x] 2.4 将右边界导航插件接入默认插件装配，确保不拦截行内公式和其他 mark。

## 3. 行为一致性核对

- [x] 3.1 核对点击或移动到标签尾部能够继续追加内容，第一次右方向键退出后输入普通文字，第二次右方向键正常移动。
- [x] 3.2 核对 Markdown 解析与序列化、其他行内格式及行内公式导航未被修改，并清理本次修正产生的孤儿代码。

## 4. 退出状态视觉提示

- [x] 4.1 在行内代码显式退出状态通过零宽 Decoration widget 渲染标签右侧指示线，不改变文档内容或 Markdown。
- [x] 4.2 在行内代码模块独立 CSS 文件中实现使用主题变量的闪烁竖线，并确保不占据布局宽度、不响应指针事件。
- [x] 4.3 在输入、第二次右方向键、重新点击标签、选区离开边界和编辑器失焦时清除退出指示线。
- [x] 4.4 静态核对指示线生命周期、主题适配、普通文字输入及行内公式和其他 mark 不受影响。

## 5. 左右边界一致性修正

- [x] 5.1 将退出状态扩展为包含左侧或右侧方向，并在对应边界使用单个零宽 Decoration 指示光标。
- [x] 5.2 退出状态激活时隐藏原生 caret，状态清除或插件销毁时恢复，消除双光标。
- [x] 5.3 拦截从行内代码第一个字符向左的移动，先将选区稳定放到标签内起点并保留 `inlineCode`，允许在开头补充内容。
- [x] 5.4 在标签内起点再次按左方向键时退出到标签左侧，支持退出后普通文字输入和第二次左方向键正常导航。
- [x] 5.5 静态核对左右边界的单光标呈现、标签首尾补充、退出状态清理以及行内公式和其他 mark 不受影响。

## 6. 边界反向重入与 DOM 落点

- [x] 6.1 在方向键分发前优先处理退出状态：同方向键继续普通向外导航，反方向键阻止原生行为并重新进入标签。
- [x] 6.2 从右侧退出状态按左方向键时恢复 `inlineCode` stored marks，并将光标精确放到标签内尾部。
- [x] 6.3 从左侧退出状态按右方向键时恢复 `inlineCode` stored marks，并将光标精确放到标签内开头。
- [x] 6.4 通过实际 `<code>` 首尾文本节点设置内部 DOM caret，并将外部 Decoration 锚定在代码外框边缘，确保左右内外位置可见区分。
- [x] 6.5 静态核对反方向重入、同方向导航、首尾追加、内外光标位置以及其他 mark 和行内公式不受影响。

## 7. 左侧父容器 DOM 映射修正

- [x] 7.1 扩展行内代码 DOM 解析：除当前位置及祖先外，根据 `domAtPos` 返回的容器 offset 检查对应子节点及其 `<code>` 后代。
- [x] 7.2 左侧标签内起点解析成功后，将原生光标精确折叠到 `<code>` 首个文本节点 offset 0，确保位于左内边距之后、首字符之前。
- [x] 7.3 保持左侧外部 Decoration 位于代码外框之外，并确认本次修正不改变已经正常的右侧退出与重入逻辑。
- [x] 7.4 核对左侧标签内开头补充、左侧退出、反向重入及内外视觉位置，并确认其他 mark、行内公式和 Markdown 内容不受影响。

## 8. 左侧标签内独立视觉状态

- [x] 8.1 将插件状态扩展为包含 `inside` 或 `outside` 位置，使左侧标签内起点与左右显式退出状态能够独立呈现。
- [x] 8.2 左侧进入标签内起点时保留 `inlineCode` stored mark，隐藏原生 caret，并创建继承相邻真实 `inlineCode` mark 的单个 Decoration 指示光标。
- [x] 8.3 标签内起点按左方向键时切换到左侧外部状态，按右方向键时清除内部状态并放行普通向内导航，输入时清除提示但继续扩展行内代码。
- [x] 8.4 更新行内代码模块样式以区分左侧内部与外部指示光标，并移除不再需要的左侧原生 DOM 强制定位逻辑，保持右侧行为不变。
- [x] 8.5 核对左侧标签内外唯一光标、开头补充、双向导航、右侧既有行为以及其他 mark、行内公式和 Markdown 内容不受影响。

## 9. 左侧标签内开头入口补全

- [x] 9.1 鼠标直接点击行内代码左侧内边距或首字符之前时，即使没有既有边界视觉状态，也创建左侧 `inside` 状态并恢复真实 `inlineCode` stored marks。
- [x] 9.2 普通文本光标位于行内代码左边界且没有视觉状态时，第一次按右方向键阻止原生导航并进入标签内开头，不得越过首字符。
- [x] 9.3 标签内开头再次按右方向键时才进入首字符之后；在该状态输入时应在标签开头补充内容并继续使用 `inlineCode` 格式。
- [x] 9.4 保持点击行内代码其他位置的原生定位、右侧边界现有退出与重入行为不变。
- [x] 9.5 核对鼠标点击开头、普通外部输入后两次右方向键、左右双向导航、其他 mark、行内公式和 Markdown 内容。

## 10. 右侧普通文字逐步进入标签

- [x] 10.1 鼠标点击标签后普通文字的第一个字符之前时，根据 DOM 点击目标将右边界明确为普通文本状态，并移除显式输入 marks 中的 `inlineCode`。
- [x] 10.2 无视觉状态的普通文本光标位于行内代码右边界时，第一次按左方向键阻止原生导航并创建右侧 `outside` 状态，不得直接进入标签内尾部。
- [x] 10.3 右侧 `outside` 状态再次按左方向键时复用既有反向重入逻辑进入标签内尾部；第一次左方向键后输入仍保持普通文本格式。
- [x] 10.4 直接点击标签内尾部后按左方向键继续普通字符导航，并保持既有右侧显式退出与反向重入行为不变。
- [x] 10.5 核对右侧普通文字两次左方向键、标签内尾部左键、右侧普通文字输入、左右边界、其他 marks、行内公式和 Markdown 内容。

## 11. 右边界鼠标几何判定

- [x] 11.1 在 `mousedown` 阶段通过 `view.posAtCoords` 解析鼠标文档位置，并取得相邻真实 `<code>` 元素的外框几何信息。
- [x] 11.2 鼠标横坐标位于代码外框右侧且右侧相邻节点为普通文本时，阻止原生选区更新，固定右边界选区并设置不包含 `inlineCode` 的普通文本 marks。
- [x] 11.3 鼠标横坐标仍位于代码背景或右内边距内时保留真实 `inlineCode` mark，使标签内尾部左方向键继续普通字符导航。
- [x] 11.4 移除右边界以 `event.target` 为主要依据的点击侧判定；几何信息无法解析或右侧不是普通文本时保守放行，并保持既有左右边界状态链不变。
- [x] 11.5 核对外框右侧点击后的两次左方向键、代码右内边距点击、普通文字输入、左右边界、其他 marks、行内公式和 Markdown 内容。

## 12. 右侧普通首字符回退落点

- [x] 12.1 识别光标位于标签后第一个普通字符之后的左方向键入口，并确认该字符之前紧邻真实 `inlineCode` 右边界。
- [x] 12.2 阻止该入口的原生左移，将选区显式移动到第一个普通字符之前，保留普通文本 marks 并创建右侧 `outside` 状态。
- [x] 12.3 根据相邻真实 `<code>` 外框校正右侧外部 Decoration，使唯一指示光标位于 `codeRect.right` 之外、不与代码背景重叠，同时隐藏原生 caret。
- [x] 12.4 保持下一次左方向键进入标签内尾部、外部状态输入普通文字、代码右内边距点击及既有左右边界行为不变。
- [x] 12.5 静态核对普通首字符之后左移、标签外部视觉位置、连续两次左键、其他 marks、行内公式和 Markdown 内容。

## 13. 标签内外间距与点击分界

- [x] 13.1 将行内代码横向内边距由 8px 收窄为 6px，并增加左右各 2px 外部间距，保持纵向内边距、圆角、颜色和字体不变。
- [x] 13.2 统一以 `<code>` border box 作为点击归属分界：背景及 6px 内边距属于标签内部，2px 外部间距及普通文字属于普通文本侧。
- [x] 13.3 为左边界补充与右边界一致的外框几何判定，确保点击左侧外部间距不会进入标签，点击左内边距仍可在标签开头补充内容。
- [x] 13.4 将左右 `outside` Decoration 定位到对应的 2px 外部间距内，确保不覆盖代码背景或相邻普通文字，并保持既有方向键状态链不变。
- [x] 13.5 静态核对左右连接处点击、标签首尾补充、外部普通文字输入、内外唯一光标、其他 marks、行内公式和 Markdown 内容。

## 14. 右边界鼠标内外落点分离

- [x] 14.1 扩展右边界候选位置解析：当 `posAtCoords` 落在首个普通字符起点或共享边界任一侧时，归一化到左侧为真实 `inlineCode`、右侧为普通文本的唯一边界。
- [x] 14.2 外框右侧命中时阻止原生选区更新，保留普通文本 marks，并立即创建位于 2px 外部间距内、带鼠标入口导航阶段的右侧 `outside` 状态。
- [x] 14.3 右内边距命中时清除右侧外部状态、恢复真实 `inlineCode` mark 和标签内尾部光标，确保内外点击显示为两个明确位置。
- [x] 14.4 调整同一次指针交互的事件衔接，使后续 `handleClick` 不得覆盖 `mousedown` 已确定的边界、stored marks 与视觉状态；鼠标入口第一次左方向键仍停留外部，第二次才进入标签内尾部。
- [x] 14.5 静态核对右内边距、2px 外部间距、首个普通字符头部、连续两次左方向键、普通文字输入、唯一光标、行内公式和 Markdown 内容。
