## 1. 搜索基础能力

- [x] 1.1 使用 pnpm 添加与当前 ProseMirror 版本兼容的 `prosemirror-search` 依赖，并保持现有依赖管理方式一致
- [x] 1.2 在 `src/plugins/custom/search/` 定义查询选项、结果快照和搜索控制器类型，并导出单一模块入口
- [x] 1.3 基于 `prosemirror-search` 实现普通文本、大小写、全词和正则查询适配，包含无效正则与空查询状态
- [x] 1.4 实现全部匹配和当前匹配装饰、结果计数、文档变化刷新及首尾循环的上一项/下一项导航
- [x] 1.5 实现代码块源码匹配定位，在命中预览模式代码块时复用现有切换入口展示源码和当前匹配

## 2. 替换与运行时集成

- [x] 2.1 实现替换当前匹配、正则捕获组展开和替换后继续导航
- [x] 2.2 实现单个 ProseMirror 事务中的全部替换，确保一次撤销可恢复且无匹配时不修改文档
- [x] 2.3 在搜索替换命令中检查 EditorView 可编辑状态，保证只读实例无法修改文档
- [x] 2.4 将搜索插件加入默认启动插件装配，并在 `createMilkdownEditorRuntime` 暴露实例级搜索控制器
- [x] 2.5 通过 `useMilkdownEditor` 和稳定 ref 将搜索控制器及结果快照连接到 React 生命周期，清理销毁后的引用和高亮
- [x] 2.6 使用 `$prose` 分别包装 `search()` 和搜索快照 ProseMirror 插件，确保 `Editor.use()` 接收 Milkdown 插件函数

## 3. React 搜索界面

- [x] 3.1 新增职责单一的 `SearchPanel`，提供搜索与替换输入、结果计数、导航、替换、大小写、全词、正则和关闭控件
- [x] 3.2 在 `MilkdownEditor` 根节点实现实例范围内的 `Ctrl/Cmd+F`、`Enter`、`Shift+Enter` 和 `Escape` 键盘分发及焦点恢复
- [x] 3.3 将搜索表单状态和插件结果快照接入 `SearchPanel`，保证同页面多个编辑器状态隔离且查询变化不重建 Milkdown
- [x] 3.4 在只读模式保留搜索与导航并移除可执行替换控件，同时处理搜索栏与只读标签的布局冲突
- [x] 3.5 扩展 `EditorI18nMessages` 及中英文资源，覆盖输入、选项、计数、错误、按钮和无障碍名称

## 4. 样式与文档

- [x] 4.1 新增 `src/styles/modules/search.css` 并由全局样式入口导入，使用现有主题变量完成高亮、当前匹配、焦点和错误状态
- [x] 4.2 完成桌面与窄屏搜索栏布局，使其固定在 `.zt-md-body`、不受编辑区滚动裁剪并满足触控尺寸要求
- [x] 4.3 更新 README 的功能、快捷键、搜索范围、替换语义、只读限制、国际化文案和 Portal 说明
- [x] 4.4 静态核对所有规范场景、文件职责和行数约束，并删除本次修改产生的未使用导入、变量或函数
- [x] 4.5 静态核对搜索插件从创建到 `Editor.use()` 的装配链，并在获得项目运行命令授权后验证编辑器可成功初始化

## 5. 搜索交互修订

- [x] 5.1 为可编辑搜索栏增加替换区展开状态和无障碍展开语义，默认收起替换输入与操作，收起时保留替换文本
- [x] 5.2 重组 `SearchPanel` 的控件层级，完整保留大小写、全词、正则、结果计数、导航、替换与关闭功能，并在只读模式隐藏替换展开入口
- [x] 5.3 调整搜索模块样式，使桌面主行紧凑、展开替换时使用第二行、窄屏无水平溢出，并根据展开状态缩减或增加编辑区顶部预留空间
- [x] 5.4 修复上一项、下一项、首尾循环和替换后导航的实例级滚动，基于当前匹配坐标显式调整当前 `.zt-md-editor.scrollTop`，确保普通文本匹配完整可见且不被搜索栏遮挡
- [x] 5.5 静态核对明暗主题、可编辑与只读、多实例、按钮与键盘导航场景，并更新 README 中的搜索栏层级和滚动行为说明
- [x] 5.6 统一替换展开、搜索选项、导航、关闭和替换操作的尺寸、圆角、分组及默认、悬停、按下、选中、聚焦、禁用状态，保持搜索输入的主视觉层级
- [x] 5.7 在预览代码块切换为源码后等待视图 DOM 更新，重新测量当前匹配并执行同一实例级滚动定位
- [x] 5.8 静态核对匹配位于视口上方、下方、首尾循环、搜索栏顶部安全区、窄屏换行和多实例隔离场景，并同步 README 的按钮层级与显式滚动说明
