## 1. 调整行内代码鼠标边界处理

- [x] 1.1 将现有行内代码边界几何判定从 `mousedown` 事件接线迁移到普通点击路径，移除按下阶段对原生选区的阻止。
- [x] 1.2 在边界点击处理前检查最终文本选区，非空选区直接放行，空选区继续复用现有标签内外 marks、选区与视觉状态逻辑。
- [x] 1.3 调整点击分发顺序，使已处理的边界普通单击不再被后续点击逻辑覆盖，并删除修改产生的孤儿事件接线或命名。

## 2. 行为核对

- [x] 2.1 核对从标签开头、结尾、左右内边距开始拖拽，以及跨相邻文字和文本块拖拽时，非空选区保持连续且格式工具正常显示。
- [x] 2.2 核对标签内外普通单击、首尾继续输入、左侧边界与标签内部方向键导航及双击选词行为保持不变。
- [x] 2.3 核对变更不修改行内代码 Markdown 内容、样式尺寸、主题变量、公开 API 或其他行内 mark 的选区行为。

## 3. 移除鼠标右侧入口的二次确认

- [x] 3.1 移除鼠标点击右侧外部边界时附加的 `requiresInwardConfirmation` 状态，并从边界状态类型中删除该孤儿属性。
- [x] 3.2 移除右侧外部状态首次向内按键只取消确认标志的吞键分支，使第一次左方向键直接复用既有标签末尾恢复逻辑。
- [x] 3.3 清理仅服务鼠标二次确认的辅助判断、导入或函数，同时保持普通单击几何判定和非空选区放行顺序不变。

## 4. 右边界导航核对

- [x] 4.1 核对鼠标点击标签右侧普通文字开头后，第一次按左方向键立即进入标签末尾并恢复 `inlineCode` 输入状态。
- [x] 4.2 核对从标签后首个普通字符之后按左方向键时仍先落到右侧外部边界，下一次左方向键才进入标签末尾。
- [x] 4.3 核对右侧外部输入普通文字、标签尾部输入、拖拽选区、双击选词、其他方向键路径和 Markdown 内容保持不变。
